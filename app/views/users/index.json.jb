@users.map do |user|
  {
    id: user.id,
    name: user.name,
    email: user.email,
    image_url: user.image_url,
    
    favorites: user.favorites,
    reviews: user.reviews,
    lists: user.lists
  }
end
